document.addEventListener('DOMContentLoaded', () => {
    console.log("Ndivhuho Electrical Works Demo Loaded");
    
    // Future Logic: If we add a Mobile Menu later, we put the code here.
    // For now, the site is simple enough to not need complex JS.
});